const newRoute = require('./new')
const checkEmailRoute = require('./check-email')
const createRoute = require('./create')

  //createRoute,
module.exports = {
  newRoute,
  checkEmailRoute,
  createRoute
}
